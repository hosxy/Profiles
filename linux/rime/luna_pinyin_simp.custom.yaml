#patch:
#  engine/filters:
#    - simplifier
#    - uniquifier
#    - cjk_minifier                        # 输入方案的 fitlers 内增加这一行
#  translator/enable_charset_filter: true  


patch:
  switches:
     - name: ascii_mode
      reset: 0
      states: ["中文", "西文"]
    - name: full_shape
      states: ["半角", "全角"]
    - name: zh_simp 
      reset: 1
      states: [ 漢字, 汉字 ]
    - name: ascii_punct
      states: ["。，", "．，"]
      
    - options: ["gbk", "utf8"]
      reset: 0
      states:
        - GBK
        - UTF-8

  engine/filters:
    - simplifier
    - uniquifier
    - charset_filter@gbk                      # 生癖字过滤
